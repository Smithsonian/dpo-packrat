#!/bin/sh

# This script is used to update CA certificate store. Currently not active, but holding
# in place until CA certificate issue is proven resolved.

# Update CA certificates
update-ca-certificates