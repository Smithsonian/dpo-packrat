overwrite: true
schema: "http://localhost:4000/graphql"
generates:
  ./graphql/schema.graphql:
    plugins:
      - schema-ast
  ./types/graphql.ts:
    plugins:
      - typescript
  ../client/src/types/graphql.ts:
    plugins:
      - typescript
