# import User from "../user/types.graphql"

scalar DateTime

type License {
  id: ID!
  name: String!
  description: String!
  licenseAssignments: [LicenseAssignment]
}

type LicenseAssignment {
  id: ID!
  license: License!
  userCreators: User
  dateStart: DateTime
  dateEnd: DateTime
}
