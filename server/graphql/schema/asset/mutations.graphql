scalar Upload

enum AssetType {
    Diconde
    Photogrammetry
}

type Mutation {
    uploadAsset(file: Upload!, type: AssetType!): UploadAssetResult!
}

enum UploadStatus {
    COMPLETE
    FAILED
}

type UploadAssetResult {
    status: UploadStatus!
}
