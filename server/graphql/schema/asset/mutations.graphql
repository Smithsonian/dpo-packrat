scalar Upload

type Mutation {
    uploadAsset(file: Upload!, type: Int!): UploadAssetResult!
}

type UploadAssetInput {
    file: Upload!
    type: Int!
}

enum UploadStatus {
    COMPLETE
    FAILED
}

type UploadAssetResult {
    status: UploadStatus!
}
