/**
 * Selectors
 *
 * These are the selectors for various elements in the client,
 * and are shared with E2E testing
 */
const Selectors = {
    AUTH: {
        EMAIL_FIELD: 'auth-email-field',
        PASSWORD_FIELD: 'auth-password-field',
        LOGIN_BUTTON: 'auth-login-button',
        LOGOUT_BUTTON: 'auth-logout-button'
    }
};

export { Selectors };
